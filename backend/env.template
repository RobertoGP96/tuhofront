# ============================================
# TUhoBackend - Variables de Entorno Django
# ============================================
# Copia este archivo a backend/.env y completa los valores según tu entorno
# IMPORTANTE: Nunca commitees el archivo .env real con valores reales

# ============================================
# SEGURIDAD (OBLIGATORIO)
# ============================================

# Clave secreta de Django (OBLIGATORIO)
# Genera una nueva clave con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# ⚠️ NUNCA uses el valor por defecto en producción
SECRET_KEY=django-insecure-CHANGE-THIS-TO-A-RANDOM-SECRET-KEY

# Modo de depuración (True para desarrollo, False para producción)
# ⚠️ SIEMPRE False en producción
DEBUG=True

# Hosts permitidos (separados por comas)
# En desarrollo: *
# En producción: tuho.uho.edu.cu,www.tuho.uho.edu.cu,127.0.0.1
ALLOWED_HOSTS=*

# Dominio del sitio
DOMAIN=127.0.0.1:8000

# ============================================
# BASE DE DATOS
# ============================================

# SQLite (desarrollo) - No requiere configuración adicional
# Se usa por defecto si no se configuran las variables de PostgreSQL

# PostgreSQL (producción recomendado) - Descomenta y configura:
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=tuho_db
# DATABASE_USER=tuho_user
# DATABASE_PASSWORD=tuho_password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# ============================================
# CORREO ELECTRÓNICO
# ============================================

# Configuración SMTP para envío de correos
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USE_TLS=False
EMAIL_USE_SSL=True
EMAIL_HOST_USER=tu_correo@gmail.com
EMAIL_HOST_PASSWORD=tu_contraseña_de_aplicacion
DEFAULT_FROM_EMAIL=noreply@uho.cu

# ============================================
# CORS (Cross-Origin Resource Sharing)
# ============================================

# Orígenes permitidos para CORS (separados por comas)
# En desarrollo: http://localhost:5173,http://localhost:3000
# En producción: https://tuho.uho.edu.cu
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=True

# ============================================
# JWT (JSON Web Tokens)
# ============================================

# Tiempo de vida del token de acceso (en minutos)
JWT_ACCESS_TOKEN_LIFETIME=60

# Tiempo de vida del token de refresco (en días)
JWT_REFRESH_TOKEN_LIFETIME=7

# ============================================
# ARCHIVOS ESTÁTICOS Y MEDIA (Opcional)
# ============================================

# Directorio para archivos subidos por usuarios (opcional)
# MEDIA_ROOT=/path/to/media

# URL para acceder a archivos media (opcional)
# MEDIA_URL=/media/

# Directorio para archivos estáticos recopilados (opcional)
# STATIC_ROOT=/path/to/staticfiles

# ============================================
# PRODUCCIÓN - Configuraciones Adicionales
# ============================================

# Solo para producción - Descomenta cuando despliegues:
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_BROWSER_XSS_FILTER=True
# SECURE_CONTENT_TYPE_NOSNIFF=True
# X_FRAME_OPTIONS=DENY

