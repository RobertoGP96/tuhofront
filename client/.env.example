# URL base de la API. Debe terminar con /api/v1 (sin slash final).
VITE_API_URL=http://localhost:8000/api/v1

# (Opcional) Correo institucional al que apunta el formulario de contacto.
# Si no se define, se usa atencion@uho.edu.cu por defecto.
VITE_CONTACT_EMAIL=atencion@uho.edu.cu
